# Development Environment Configuration
spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/clientmanagement
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:azerty}

# Enable detailed health information for development
management.endpoint.health.show-details=always

jwt.secret=c2VjcmV0X2tleV9mb3JfdGVzdGluZ19vbmx5
jwt.expiration=3600000